"""
Tests for the functions
"""
import functions
#import maya.cmds as cmds
#from functions import createCircleCtrl()

#assert(createCircleCtrl("ABC",5) == )

#from functions import createLollipopCtrl

#assert(lollipopCtrl == lollipopCtrlName)



#the iterator stuff:
#assert(0,30,60] ==list(CircleIterator(3)))

# test the virtual circle generation 

#for x in xrange(numberOfVerticalCircles):
#    verticalCircle = cmds.circle(nr=(1, 0, 0), r=radiusLolli, n=verticalCircleName)[0]
#    cmds.rotate(0, angleVertical * x, 0)
#    print (verticalCircle)
#
#    # delete history
#    cmds.delete(verticalCircle, constructionHistory=True)
#    # freeze transforms
#    cmds.makeIdentity(verticalCircle, apply=True, t=1, r=1, s=1, n=2)
#
#    circleList.append(verticalCircle)

# okay I give up, for the future try do unit tests